# Project Backlog

_This file owns unresolved project work only. Completed behavior belongs in `CHANGELOG.md`; durable contracts belong in `AGENTS.md` and `/docs`._

- [ ] [`Thread display modes and reclaimable global slots`](./docs/multi-instance-bus.md#approved-next-contract-directory-names-and-reclaimable-slots): Add profile-wide display modes ordered Letters, Names (default), Directories; replace dual letter identities with profile-wide `a`–`z` slots while retaining stable names across mode changes. Preserve exact-cwd identity, role-independent continuity, authenticated routing, and accepted work; reclaim the oldest safely inactive binding only under full capacity pressure; elapsed time alone never retires a binding. No live deletion, release, or commit belongs to this implementation scope.
  - [ ] `Reclamation checkpoint`: The fail-closed authority snapshot combines local operations with separate external-owner, accepted-work, and delivery-authority evidence. `workspace-retirement` selects the oldest pressure victim only when all slots are occupied or reserved, counts standalone reservations, rechecks authority/protection, and persists one exact profile/epoch-fenced intent. Workspace bindings now accumulate historical follower journal keys; fresh bindings mark the set complete, while legacy migration cannot invent completeness. The accepted-work owner reads the shared leader journal plus every known follower journal and treats missing/unreadable/incomplete coverage as unknown. Known empty follower-journal keys can now be pruned only from a complete readable capture, with separate writer/process quiescence, exact binding, profile, and leader-epoch fences; nonempty, unreadable, or writable keys remain. `journal` now discovers canonical profile-exact follower snapshots/segment roots and fails incomplete on unexpected matching paths; `workspace-retirement` can read discovered journals as target-scoped evidence while keeping legacy completeness unset. The isolated executor now requires the caller-owned gate plus durable admission ledger, acquires or adopts the exact fence, rechecks protection after admission closes, persists `deletion-issued` before invoking an executor-only one-shot deletion permit, retains unknown outcomes without reissue, advances only confirmed absence to `commit-ready`, and completes the fence after store-owned durable retirement; failed persistence retains recovery authority. A successor leader can now adopt one stale-epoch intent only when profile and complete binding snapshot match and protection remains eligible; adoption is durable, preserves original request time, and restores the old epoch on known persistence failure. One failure-resilient Workspace operation runtime now owns the gate shared by topic lifecycle, reroute restore/reclaim, leader/follower provisioning, disconnect/dead cleanup, rename, and display reconciliation; each mutation acquires cross-process admission before that gate, and follower registration waits outside the registry while provisioning is gated. The executor uses the exposed same gate. Leader assembly now exposes live external protection composed from exact registry targets, active/queued targets, shared and historical journals, and profile-exact legacy discovery; missing queue targets and unreadable/incomplete evidence remain unknown. Exact JSON and multipart Bot API requests are now counted at the common transport boundary from issuance through final settlement, including retry waits and follower-proxied publication; external protection reports only matching in-flight targets and clears after resolve/reject. Historical journal keys now resolve process-birth liveness: exact live writers protect, proven-dead identities clear, and opaque/live fallback or unverifiable platform identities stay unknown; incomplete legacy key history also stays unknown. The independent review's journal/API admission race is closed in ledger/executor, adapter, and production-composition tests, while production pressure retry and the leader retirement entrypoint remain disconnected pending remaining mutation coverage and re-review. Store intents now block matching claims, activation, title/journal commits, and binding upserts; global allocation includes retained bindings and transient claims; fresh/carried provisioning, follower publication/replacement, and routing recovery require a proven slot; duplicate direct binding commits fail closed; historical names cannot rewrite slots; message-scoped edits/deletes conservatively protect their whole chat; wrapped follower owner keys decode before process-birth checks; and closed topics no longer count as confirmed absence. The isolated `workspace-admission` durable reader/writer ledger now provides exact/chat/profile leases, one exact phased retirement fence, proven-dead process-birth cleanup, malformed/ambiguous-state fail-closed handling, exact idempotent recovery/release, successor adoption, and a one-shot deletion permit, with focused and real-process contention/crash regressions. Production leader/follower/recipient journal stores now resolve admission with conservative batch scope classification and lease coverage through publication. The production common JSON/multipart client resolves admission per current bot/profile, holds target/chat leases through transport settlement, treats malformed declared targets profile-wide, blocks issuance under a fence, and preserves settled request outcomes when lease release is uncertain. Production leader assembly resolves chat-wide admission across leader provisioning and profile-wide admission across follower provisioning/registration, disconnect/dead cleanup, leader/follower rename, and display preference/reconciliation, rejecting a fence before its process-local gate and mutation. Retained fences now project their uppercase slot into optional Thread-store reservations, blocking generic allocation, Workspace claims, and reclamation snapshots after binding commit; malformed or unreadable evidence fails closed. A profile runtime resolves `workspace-admission[.<profile>].json` with token-hash identity, stable named-profile switching, empty-ledger token rebind, and active-authority rotation rejection. Admission and retained-slot composition are production-wired through leader/follower mutations and exact stale-target recovery; journal-evidence pruning now requires exact-target admission through durable publication. The unanimous independent quorum found that manual disconnect/session-restart cleanup could publish an intent before the admitted API boundary; both entrypoints now acquire profile admission before the shared leader gate and hold it through cleanup settlement, with retained `fenced` and `deletion-issued` regressions. Concurrent reuse of one live operation ID is now rejected at both the common async admission runner and API adapter before a second caller can share/release the first lease; deterministic held-operation regressions prove retirement remains blocked until the original caller settles, while sequential/ambiguous recovery remains distinct. The quorum's topic-lifecycle and reroute restore/reclaim paths now use that same profile-wide runtime before store load/mutation; retained `fenced` and `deletion-issued` regressions stop state, API, identity, and durable-store effects. The production mutation inventory is now explicit: shared-runtime owners cover lifecycle, all unbound/reroute handling, cleanup, leader provisioning/registration/rename/display; dedicated leases cover journals/API, follower promotion/replacement, stale recovery, and journal pruning; status/bot-mode writes do not mutate Thread/Workspace authority. A unanimous same-model quorum then found detached post-provision reconciliation releasing the registration lease before delayed API/store settlement (`leasesWhileBackgroundBlocked: 0`). That timer-owned cleanup now reacquires fresh profile admission through the same gate; held-cleanup and both retained-fence-phase regressions prove the lease remains protective or mutation never begins. Generic Thread-store primitives remain policy-free. The independent post-fix quorum returned 2/2 PASS at 0.96 confidence per same-model reviewer: fresh admission spans delayed cleanup settlement, both fence phases stop effects, production has no fallback bypass, and registration remains free of nested-gate deadlock. This clears the admission-composition blocker without authorizing live retirement; pressure deletion remains disconnected for this release scope and pending disposable operator smoke. Post-fix `npm run validate` (typecheck, full tests, zero-vulnerability audit, dry-run package), Domain DAG, context validation, and `git diff --check` all pass. Disposable operator/mobile and Windows smoke remain before release.
  - [ ] `Evidence-led bus/storage optimization`: Measure isolated registration, heartbeat, route lookup, and snapshot read/parse/clone/write work at representative profile sizes. Retain only demonstrated simplifications; avoid speculative secondary indexes or persistent IPC multiplexing at the 26-slot bound. Preserve exact owner/generation checks, journal authority, and unknown-ACK behavior; keep diagnostics separate from recovery state and report deterministic work counts separately from latency/throughput claims.
  - [x] `Integration checkpoint`: Reconciled protocol capabilities, migration, and public documentation. Post-fix full validation, zero-vulnerability audit, dry-run package, Domain DAG, context validation, diff hygiene, and a 2/2 independent same-model quorum pass; unrelated worktree changes remain preserved. Operator/mobile and Windows evidence remain separate.
  - [ ] Complete the [0.45.0 disposable operator acceptance](./docs/multi-instance-bus.md#0450-disposable-operator-acceptance) for naming/display convergence, leader/follower routing and rename, same-directory concurrency, restore-only restart, unbound forward/Restore, promotion continuity, cleanup failure retention, and confirmation that pressure retirement remains disconnected. Capture mobile/client evidence; complete the linked native Windows checklist separately.
- [ ] `Show Me ownership transfer`: Publish pi-telegram with its bundled `show-me` Skill before publishing `@llblab/skills` without the former copy, then update Pi Kit only after both packages are available. The temporary overlap may warn and retain the first-discovered old Skill, but avoids a provider gap; the coordinated final install must expose exactly one identity. Treat the standalone Skills-package removal as breaking unless its release policy establishes otherwise.
- [ ] `Live-thread continuity and recovery`: Make loss and restoration of a live thread truthful and safe without discarding accepted work or deleting a currently owned target; the initial cause of the observed stale-thread API failures remains unproven.
  - [ ] Operator-validate that the 15-second stale grace prevents false routing gaps without weakening generation authority or leader election. Local incident replay proved the prior `ETIMEDOUT` / `EPIPE` churn was consistent with a follower event-loop stall: the response deadline won before an already-buffered leader acknowledgement could be consumed. Local IPC now gives pending socket input one poll phase before declaring timeout; genuinely silent peers retain the same bounded failure.
  - [ ] Complete the remaining operator-coordinated live smoke beyond the operator-confirmed successful leader Restore: verify follower Restore, inaccessible callbacks, already-absent chooser cleanup, direct stale-target diagnostics, and preservation of accepted local work/active-turn target. Use disposable test threads. The successful leader flow does not independently prove these failure paths or establish the initial stale-thread failure cause.
- [ ] `Native in-body controls live smoke`: Extend the operator-confirmed successful current-client smoke (singleton CML and mixed JSON/CML rows between paragraphs, ordinary prompt callbacks, labeled disabled control, separate footer callback, and HTML-mode footer fallback) to an independently identified second client, follower routing, blank disabled cells, and app-method dispatch/revision rejection. Do not infer coverage from the confirmed ordinary-button flow; in-body selection uses callback acknowledgement, not body recoloring.
- [ ] `Operator-confirmed pairing`: Replace first-contact automatic ownership with an explicit pending request that the trusted Pi interface can allow or reject before `allowedUserId` is persisted or any Telegram prompt executes; retain manual preconfiguration, bound pending-request lifetime and multiplicity, fail closed across session/transport replacement, and cover unauthorized-first-contact races.
- [ ] `Environment-backed bot tokens`: Let each profile store an exact Pi-native `$ENV_VAR` or `${ENV_VAR}` reference in `telegram.json` instead of copying the resolved secret; preserve the originating alias when `/telegram-setup` prefills from a supported environment variable, resolve only at validation/activation boundaries, fail closed with a redacted named-variable diagnostic when unresolved, retain literal-token compatibility, and cover named profiles, reload, persistence, status, and secret-redaction paths.
- [ ] [`Inference bypass Generative Apps`](./docs/generative-apps.md): Let the agent install and bind one managed JavaScript app owner for a generated-prompt prefix so deterministic controls can bypass model inference without creating another button grammar.
  - [ ] Harden the implemented `telegram_bind` installation/invocation kernel with removal operations, stronger cross-process replacement recovery, and bounded diagnostics while preserving lifecycle cancellation, worker-isolated methods, explicit staged replacement, canonical `<agent-dir>/genapps/<app>/<app>.mjs` identity, mandatory named `init`, no manifest/package metadata, non-symlink roots/sources, and fail-closed silent replacement.
  - [ ] Harden the implemented pre-queue `app::method` / strict-JSON bound-action route with revision capture for agent-mediated initial surfaces, profile/target authority, follower transport evidence, voice output delivery, and commit-unknown diagnostics while preserving generation-plus-revision stale-click rejection, fail-closed malformed/absent methods, ordinary model prompts, and native single-colon callbacks.
  - [ ] Harden the implemented cross-process transition lock, dead-owner recovery, expected-generation/revision comparison, repaired partial-tail/current-state recovery, transactional `init` reset, and output-only methods with process-birth proof, bounded lock diagnostics, more interruption points, and explicit commit-unknown evidence.
  - [ ] Complete the capability-owned Music Player adapter evidence beyond the successful real `ffplay` singleton install and no-model-turn bound `next`, `pause`, resume/`play`, and terminal `stop` Controls: exercise generated relative-volume controls over arbitrary absolute Actor percentages, `toggle`, `previous`, `status`, compatible singleton reuse, checkpoint restart, missing/terminal Run, unavailable backend, process timeout/cancellation/stream bounds, and redacted errors while keeping actor reality authoritative and rejecting generic remote-terminal methods.
  - [ ] Extend the implemented new-message default plus opt-in `viewMode: "edit"` bound-action update into optional output-only `refresh` scheduling from `refreshAfterMs`, clamped to at least two seconds and serialized after prior completion; retain one latest logical view handle per app/profile/target, skip unchanged frame digests, honor Telegram retry/backoff, cancel on lifecycle replacement, stop and forget the handle on known deletion or message-not-found, and never recreate a deleted live view without a fresh user action.
  - [ ] Cover CML and JSON equivalence, both mutually exclusive Tool shapes, direct agent-authored app discovery, install/copy and existing-app `init`, scalar/object/no-argument methods, agent-side diagnostic invocation, bounded CLI adaptation, refresh coalescing/rate limits/backoff/deletion, identity/path traversal, duplicate/overlapping prefixes, state recovery, handler failure, stale clicks, session replacement, follower routing, and the invariant that bound actions perform no model turn.

